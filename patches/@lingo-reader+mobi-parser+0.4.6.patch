diff --git a/node_modules/@lingo-reader/mobi-parser/dist/index.browser.js b/node_modules/@lingo-reader/mobi-parser/dist/index.browser.js
index 98779ed..8492bdd 100644
--- a/node_modules/@lingo-reader/mobi-parser/dist/index.browser.js
+++ b/node_modules/@lingo-reader/mobi-parser/dist/index.browser.js
@@ -1081,18 +1081,23 @@ class Kf8 {
   }
   replace(str) {
     const cssUrls = [];
-    const head = str.match(/<head[^>]*>([\s\S]*)<\/head>/i)[1];
+    const headMatch = str.match(/<head[^>]*>([\s\S]*)<\/head>/i);
+    const head = headMatch ? headMatch[1] : "";
     const links = head.match(/<link[^>]*>/gi) ?? [];
     for (const link of links) {
-      const linkHref = link.match(/href="([^"]*)"/i)[1];
-      const id = link.match(kindleResourceRegex)[2];
+      const linkHrefMatch = link.match(/href="([^"]*)"/i);
+      const idMatch = link.match(kindleResourceRegex);
+      if (!linkHrefMatch || !idMatch) continue;
+      const linkHref = linkHrefMatch[1];
+      const id = idMatch[2];
       const href = this.replaceResources(linkHref);
       cssUrls.push({
         id,
         href
       });
     }
-    const body = str.match(/<body[^>]*>([\s\S]*)<\/body>/i)[1];
+    const bodyMatch = str.match(/<body[^>]*>([\s\S]*)<\/body>/i);
+    const body = bodyMatch ? bodyMatch[1] : str;
     const bodyReplaced = this.replaceResources(body);
     return {
       html: bodyReplaced,
@@ -1228,15 +1233,18 @@ class Mobi {
     const referenceStr = firstChapterText.slice(0, bodyOpenTagIndex);
     const tocChapterStr = this.findTocChapter(referenceStr);
     if (tocChapterStr) {
-      const wrappedChapterStr = `<wrapper>${tocChapterStr.text.replace(/filepos=(\d+)/gi, 'filepos="$1"')}</wrapper>`;
-      const tocAst = await shared.parsexml(wrappedChapterStr, {
-        preserveChildrenOrder: true,
-        explicitChildren: true,
-        childkey: "children"
-      });
-      const toc = [];
-      this.parseNavMap(tocAst.wrapper.children, toc);
-      this.toc = toc;
+      try {
+        const wrappedChapterStr = `<wrapper>${tocChapterStr.text.replace(/filepos=(\d+)/gi, 'filepos="$1"')}</wrapper>`;
+        const tocAst = await shared.parsexml(wrappedChapterStr, {
+          preserveChildrenOrder: true,
+          explicitChildren: true,
+          childkey: "children"
+        });
+        const toc = [];
+        this.parseNavMap(tocAst.wrapper.children, toc);
+        this.toc = toc;
+      } catch {
+      }
     }
   }
   findTocChapter(referenceStr) {
@@ -1295,17 +1303,22 @@ class Mobi {
     html = html.replace(
       /<img[^>]*>/g,
       (matched) => {
-        const recindex = matched.match(this.recindexReg)[1];
-        const url = this.loadResource(Number.parseInt(recindex));
+        const recindexMatch = matched.match(this.recindexReg);
+        if (!recindexMatch) {
+          return matched;
+        }
+        const url = this.loadResource(Number.parseInt(recindexMatch[1]));
         return matched.replace(this.recindexReg, `src="${url}"`);
       }
     );
     html = html.replace(
       /<(video|audio)[^>]*>/g,
       (matched) => {
-        const mediarecindex = matched.match(this.recindexReg)[1];
-        const mediaUrl = this.loadResource(Number.parseInt(mediarecindex));
-        matched = matched.replace(this.mediarecindexReg, `src="${mediaUrl}"`);
+        const mediarecindexMatch = matched.match(this.recindexReg);
+        if (mediarecindexMatch) {
+          const mediaUrl = this.loadResource(Number.parseInt(mediarecindexMatch[1]));
+          matched = matched.replace(this.mediarecindexReg, `src="${mediaUrl}"`);
+        }
         const recindex = matched.match(this.recindexReg)?.[1];
         if (recindex) {
           const posterUrl = this.loadResource(Number.parseInt(recindex));
diff --git a/node_modules/@lingo-reader/mobi-parser/dist/index.browser.mjs b/node_modules/@lingo-reader/mobi-parser/dist/index.browser.mjs
index 3839370..0881e62 100644
--- a/node_modules/@lingo-reader/mobi-parser/dist/index.browser.mjs
+++ b/node_modules/@lingo-reader/mobi-parser/dist/index.browser.mjs
@@ -1079,18 +1079,23 @@ class Kf8 {
   }
   replace(str) {
     const cssUrls = [];
-    const head = str.match(/<head[^>]*>([\s\S]*)<\/head>/i)[1];
+    const headMatch = str.match(/<head[^>]*>([\s\S]*)<\/head>/i);
+    const head = headMatch ? headMatch[1] : "";
     const links = head.match(/<link[^>]*>/gi) ?? [];
     for (const link of links) {
-      const linkHref = link.match(/href="([^"]*)"/i)[1];
-      const id = link.match(kindleResourceRegex)[2];
+      const linkHrefMatch = link.match(/href="([^"]*)"/i);
+      const idMatch = link.match(kindleResourceRegex);
+      if (!linkHrefMatch || !idMatch) continue;
+      const linkHref = linkHrefMatch[1];
+      const id = idMatch[2];
       const href = this.replaceResources(linkHref);
       cssUrls.push({
         id,
         href
       });
     }
-    const body = str.match(/<body[^>]*>([\s\S]*)<\/body>/i)[1];
+    const bodyMatch = str.match(/<body[^>]*>([\s\S]*)<\/body>/i);
+    const body = bodyMatch ? bodyMatch[1] : str;
     const bodyReplaced = this.replaceResources(body);
     return {
       html: bodyReplaced,
@@ -1226,15 +1231,18 @@ class Mobi {
     const referenceStr = firstChapterText.slice(0, bodyOpenTagIndex);
     const tocChapterStr = this.findTocChapter(referenceStr);
     if (tocChapterStr) {
-      const wrappedChapterStr = `<wrapper>${tocChapterStr.text.replace(/filepos=(\d+)/gi, 'filepos="$1"')}</wrapper>`;
-      const tocAst = await parsexml(wrappedChapterStr, {
-        preserveChildrenOrder: true,
-        explicitChildren: true,
-        childkey: "children"
-      });
-      const toc = [];
-      this.parseNavMap(tocAst.wrapper.children, toc);
-      this.toc = toc;
+      try {
+        const wrappedChapterStr = `<wrapper>${tocChapterStr.text.replace(/filepos=(\d+)/gi, 'filepos="$1"')}</wrapper>`;
+        const tocAst = await parsexml(wrappedChapterStr, {
+          preserveChildrenOrder: true,
+          explicitChildren: true,
+          childkey: "children"
+        });
+        const toc = [];
+        this.parseNavMap(tocAst.wrapper.children, toc);
+        this.toc = toc;
+      } catch {
+      }
     }
   }
   findTocChapter(referenceStr) {
@@ -1293,17 +1301,22 @@ class Mobi {
     html = html.replace(
       /<img[^>]*>/g,
       (matched) => {
-        const recindex = matched.match(this.recindexReg)[1];
-        const url = this.loadResource(Number.parseInt(recindex));
+        const recindexMatch = matched.match(this.recindexReg);
+        if (!recindexMatch) {
+          return matched;
+        }
+        const url = this.loadResource(Number.parseInt(recindexMatch[1]));
         return matched.replace(this.recindexReg, `src="${url}"`);
       }
     );
     html = html.replace(
       /<(video|audio)[^>]*>/g,
       (matched) => {
-        const mediarecindex = matched.match(this.recindexReg)[1];
-        const mediaUrl = this.loadResource(Number.parseInt(mediarecindex));
-        matched = matched.replace(this.mediarecindexReg, `src="${mediaUrl}"`);
+        const mediarecindexMatch = matched.match(this.recindexReg);
+        if (mediarecindexMatch) {
+          const mediaUrl = this.loadResource(Number.parseInt(mediarecindexMatch[1]));
+          matched = matched.replace(this.mediarecindexReg, `src="${mediaUrl}"`);
+        }
         const recindex = matched.match(this.recindexReg)?.[1];
         if (recindex) {
           const posterUrl = this.loadResource(Number.parseInt(recindex));
diff --git a/node_modules/@lingo-reader/mobi-parser/dist/index.node.js b/node_modules/@lingo-reader/mobi-parser/dist/index.node.js
index 9d9bb0f..e4ab438 100644
--- a/node_modules/@lingo-reader/mobi-parser/dist/index.node.js
+++ b/node_modules/@lingo-reader/mobi-parser/dist/index.node.js
@@ -1121,18 +1121,23 @@ class Kf8 {
   }
   replace(str) {
     const cssUrls = [];
-    const head = str.match(/<head[^>]*>([\s\S]*)<\/head>/i)[1];
+    const headMatch = str.match(/<head[^>]*>([\s\S]*)<\/head>/i);
+    const head = headMatch ? headMatch[1] : "";
     const links = head.match(/<link[^>]*>/gi) ?? [];
     for (const link of links) {
-      const linkHref = link.match(/href="([^"]*)"/i)[1];
-      const id = link.match(kindleResourceRegex)[2];
+      const linkHrefMatch = link.match(/href="([^"]*)"/i);
+      const idMatch = link.match(kindleResourceRegex);
+      if (!linkHrefMatch || !idMatch) continue;
+      const linkHref = linkHrefMatch[1];
+      const id = idMatch[2];
       const href = this.replaceResources(linkHref);
       cssUrls.push({
         id,
         href
       });
     }
-    const body = str.match(/<body[^>]*>([\s\S]*)<\/body>/i)[1];
+    const bodyMatch = str.match(/<body[^>]*>([\s\S]*)<\/body>/i);
+    const body = bodyMatch ? bodyMatch[1] : str;
     const bodyReplaced = this.replaceResources(body);
     return {
       html: bodyReplaced,
@@ -1274,15 +1279,18 @@ class Mobi {
     const referenceStr = firstChapterText.slice(0, bodyOpenTagIndex);
     const tocChapterStr = this.findTocChapter(referenceStr);
     if (tocChapterStr) {
-      const wrappedChapterStr = `<wrapper>${tocChapterStr.text.replace(/filepos=(\d+)/gi, 'filepos="$1"')}</wrapper>`;
-      const tocAst = await shared.parsexml(wrappedChapterStr, {
-        preserveChildrenOrder: true,
-        explicitChildren: true,
-        childkey: "children"
-      });
-      const toc = [];
-      this.parseNavMap(tocAst.wrapper.children, toc);
-      this.toc = toc;
+      try {
+        const wrappedChapterStr = `<wrapper>${tocChapterStr.text.replace(/filepos=(\d+)/gi, 'filepos="$1"')}</wrapper>`;
+        const tocAst = await shared.parsexml(wrappedChapterStr, {
+          preserveChildrenOrder: true,
+          explicitChildren: true,
+          childkey: "children"
+        });
+        const toc = [];
+        this.parseNavMap(tocAst.wrapper.children, toc);
+        this.toc = toc;
+      } catch {
+      }
     }
   }
   findTocChapter(referenceStr) {
@@ -1341,17 +1349,22 @@ class Mobi {
     html = html.replace(
       /<img[^>]*>/g,
       (matched) => {
-        const recindex = matched.match(this.recindexReg)[1];
-        const url = this.loadResource(Number.parseInt(recindex));
+        const recindexMatch = matched.match(this.recindexReg);
+        if (!recindexMatch) {
+          return matched;
+        }
+        const url = this.loadResource(Number.parseInt(recindexMatch[1]));
         return matched.replace(this.recindexReg, `src="${url}"`);
       }
     );
     html = html.replace(
       /<(video|audio)[^>]*>/g,
       (matched) => {
-        const mediarecindex = matched.match(this.recindexReg)[1];
-        const mediaUrl = this.loadResource(Number.parseInt(mediarecindex));
-        matched = matched.replace(this.mediarecindexReg, `src="${mediaUrl}"`);
+        const mediarecindexMatch = matched.match(this.recindexReg);
+        if (mediarecindexMatch) {
+          const mediaUrl = this.loadResource(Number.parseInt(mediarecindexMatch[1]));
+          matched = matched.replace(this.mediarecindexReg, `src="${mediaUrl}"`);
+        }
         const recindex = matched.match(this.recindexReg)?.[1];
         if (recindex) {
           const posterUrl = this.loadResource(Number.parseInt(recindex));
diff --git a/node_modules/@lingo-reader/mobi-parser/dist/index.node.mjs b/node_modules/@lingo-reader/mobi-parser/dist/index.node.mjs
index ea2a772..6985468 100644
--- a/node_modules/@lingo-reader/mobi-parser/dist/index.node.mjs
+++ b/node_modules/@lingo-reader/mobi-parser/dist/index.node.mjs
@@ -1119,18 +1119,23 @@ class Kf8 {
   }
   replace(str) {
     const cssUrls = [];
-    const head = str.match(/<head[^>]*>([\s\S]*)<\/head>/i)[1];
+    const headMatch = str.match(/<head[^>]*>([\s\S]*)<\/head>/i);
+    const head = headMatch ? headMatch[1] : "";
     const links = head.match(/<link[^>]*>/gi) ?? [];
     for (const link of links) {
-      const linkHref = link.match(/href="([^"]*)"/i)[1];
-      const id = link.match(kindleResourceRegex)[2];
+      const linkHrefMatch = link.match(/href="([^"]*)"/i);
+      const idMatch = link.match(kindleResourceRegex);
+      if (!linkHrefMatch || !idMatch) continue;
+      const linkHref = linkHrefMatch[1];
+      const id = idMatch[2];
       const href = this.replaceResources(linkHref);
       cssUrls.push({
         id,
         href
       });
     }
-    const body = str.match(/<body[^>]*>([\s\S]*)<\/body>/i)[1];
+    const bodyMatch = str.match(/<body[^>]*>([\s\S]*)<\/body>/i);
+    const body = bodyMatch ? bodyMatch[1] : str;
     const bodyReplaced = this.replaceResources(body);
     return {
       html: bodyReplaced,
@@ -1272,15 +1277,18 @@ class Mobi {
     const referenceStr = firstChapterText.slice(0, bodyOpenTagIndex);
     const tocChapterStr = this.findTocChapter(referenceStr);
     if (tocChapterStr) {
-      const wrappedChapterStr = `<wrapper>${tocChapterStr.text.replace(/filepos=(\d+)/gi, 'filepos="$1"')}</wrapper>`;
-      const tocAst = await parsexml(wrappedChapterStr, {
-        preserveChildrenOrder: true,
-        explicitChildren: true,
-        childkey: "children"
-      });
-      const toc = [];
-      this.parseNavMap(tocAst.wrapper.children, toc);
-      this.toc = toc;
+      try {
+        const wrappedChapterStr = `<wrapper>${tocChapterStr.text.replace(/filepos=(\d+)/gi, 'filepos="$1"')}</wrapper>`;
+        const tocAst = await parsexml(wrappedChapterStr, {
+          preserveChildrenOrder: true,
+          explicitChildren: true,
+          childkey: "children"
+        });
+        const toc = [];
+        this.parseNavMap(tocAst.wrapper.children, toc);
+        this.toc = toc;
+      } catch {
+      }
     }
   }
   findTocChapter(referenceStr) {
@@ -1339,17 +1347,22 @@ class Mobi {
     html = html.replace(
       /<img[^>]*>/g,
       (matched) => {
-        const recindex = matched.match(this.recindexReg)[1];
-        const url = this.loadResource(Number.parseInt(recindex));
+        const recindexMatch = matched.match(this.recindexReg);
+        if (!recindexMatch) {
+          return matched;
+        }
+        const url = this.loadResource(Number.parseInt(recindexMatch[1]));
         return matched.replace(this.recindexReg, `src="${url}"`);
       }
     );
     html = html.replace(
       /<(video|audio)[^>]*>/g,
       (matched) => {
-        const mediarecindex = matched.match(this.recindexReg)[1];
-        const mediaUrl = this.loadResource(Number.parseInt(mediarecindex));
-        matched = matched.replace(this.mediarecindexReg, `src="${mediaUrl}"`);
+        const mediarecindexMatch = matched.match(this.recindexReg);
+        if (mediarecindexMatch) {
+          const mediaUrl = this.loadResource(Number.parseInt(mediarecindexMatch[1]));
+          matched = matched.replace(this.mediarecindexReg, `src="${mediaUrl}"`);
+        }
         const recindex = matched.match(this.recindexReg)?.[1];
         if (recindex) {
           const posterUrl = this.loadResource(Number.parseInt(recindex));
