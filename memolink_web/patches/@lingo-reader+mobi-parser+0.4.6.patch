diff --git a/node_modules/@lingo-reader/mobi-parser/dist/index.browser.js b/node_modules/@lingo-reader/mobi-parser/dist/index.browser.js
index 98779ed..3f49ceb 100644
--- a/node_modules/@lingo-reader/mobi-parser/dist/index.browser.js
+++ b/node_modules/@lingo-reader/mobi-parser/dist/index.browser.js
@@ -1228,15 +1228,18 @@ class Mobi {
     const referenceStr = firstChapterText.slice(0, bodyOpenTagIndex);
     const tocChapterStr = this.findTocChapter(referenceStr);
     if (tocChapterStr) {
-      const wrappedChapterStr = `<wrapper>${tocChapterStr.text.replace(/filepos=(\d+)/gi, 'filepos="$1"')}</wrapper>`;
-      const tocAst = await shared.parsexml(wrappedChapterStr, {
-        preserveChildrenOrder: true,
-        explicitChildren: true,
-        childkey: "children"
-      });
-      const toc = [];
-      this.parseNavMap(tocAst.wrapper.children, toc);
-      this.toc = toc;
+      try {
+        const wrappedChapterStr = `<wrapper>${tocChapterStr.text.replace(/filepos=(\d+)/gi, 'filepos="$1"')}</wrapper>`;
+        const tocAst = await shared.parsexml(wrappedChapterStr, {
+          preserveChildrenOrder: true,
+          explicitChildren: true,
+          childkey: "children"
+        });
+        const toc = [];
+        this.parseNavMap(tocAst.wrapper.children, toc);
+        this.toc = toc;
+      } catch {
+      }
     }
   }
   findTocChapter(referenceStr) {
diff --git a/node_modules/@lingo-reader/mobi-parser/dist/index.browser.mjs b/node_modules/@lingo-reader/mobi-parser/dist/index.browser.mjs
index 3839370..7a53dcb 100644
--- a/node_modules/@lingo-reader/mobi-parser/dist/index.browser.mjs
+++ b/node_modules/@lingo-reader/mobi-parser/dist/index.browser.mjs
@@ -1226,15 +1226,18 @@ class Mobi {
     const referenceStr = firstChapterText.slice(0, bodyOpenTagIndex);
     const tocChapterStr = this.findTocChapter(referenceStr);
     if (tocChapterStr) {
-      const wrappedChapterStr = `<wrapper>${tocChapterStr.text.replace(/filepos=(\d+)/gi, 'filepos="$1"')}</wrapper>`;
-      const tocAst = await parsexml(wrappedChapterStr, {
-        preserveChildrenOrder: true,
-        explicitChildren: true,
-        childkey: "children"
-      });
-      const toc = [];
-      this.parseNavMap(tocAst.wrapper.children, toc);
-      this.toc = toc;
+      try {
+        const wrappedChapterStr = `<wrapper>${tocChapterStr.text.replace(/filepos=(\d+)/gi, 'filepos="$1"')}</wrapper>`;
+        const tocAst = await parsexml(wrappedChapterStr, {
+          preserveChildrenOrder: true,
+          explicitChildren: true,
+          childkey: "children"
+        });
+        const toc = [];
+        this.parseNavMap(tocAst.wrapper.children, toc);
+        this.toc = toc;
+      } catch {
+      }
     }
   }
   findTocChapter(referenceStr) {
diff --git a/node_modules/@lingo-reader/mobi-parser/dist/index.node.js b/node_modules/@lingo-reader/mobi-parser/dist/index.node.js
index 9d9bb0f..9e4420d 100644
--- a/node_modules/@lingo-reader/mobi-parser/dist/index.node.js
+++ b/node_modules/@lingo-reader/mobi-parser/dist/index.node.js
@@ -1274,15 +1274,18 @@ class Mobi {
     const referenceStr = firstChapterText.slice(0, bodyOpenTagIndex);
     const tocChapterStr = this.findTocChapter(referenceStr);
     if (tocChapterStr) {
-      const wrappedChapterStr = `<wrapper>${tocChapterStr.text.replace(/filepos=(\d+)/gi, 'filepos="$1"')}</wrapper>`;
-      const tocAst = await shared.parsexml(wrappedChapterStr, {
-        preserveChildrenOrder: true,
-        explicitChildren: true,
-        childkey: "children"
-      });
-      const toc = [];
-      this.parseNavMap(tocAst.wrapper.children, toc);
-      this.toc = toc;
+      try {
+        const wrappedChapterStr = `<wrapper>${tocChapterStr.text.replace(/filepos=(\d+)/gi, 'filepos="$1"')}</wrapper>`;
+        const tocAst = await shared.parsexml(wrappedChapterStr, {
+          preserveChildrenOrder: true,
+          explicitChildren: true,
+          childkey: "children"
+        });
+        const toc = [];
+        this.parseNavMap(tocAst.wrapper.children, toc);
+        this.toc = toc;
+      } catch {
+      }
     }
   }
   findTocChapter(referenceStr) {
diff --git a/node_modules/@lingo-reader/mobi-parser/dist/index.node.mjs b/node_modules/@lingo-reader/mobi-parser/dist/index.node.mjs
index ea2a772..22b1dd6 100644
--- a/node_modules/@lingo-reader/mobi-parser/dist/index.node.mjs
+++ b/node_modules/@lingo-reader/mobi-parser/dist/index.node.mjs
@@ -1272,15 +1272,18 @@ class Mobi {
     const referenceStr = firstChapterText.slice(0, bodyOpenTagIndex);
     const tocChapterStr = this.findTocChapter(referenceStr);
     if (tocChapterStr) {
-      const wrappedChapterStr = `<wrapper>${tocChapterStr.text.replace(/filepos=(\d+)/gi, 'filepos="$1"')}</wrapper>`;
-      const tocAst = await parsexml(wrappedChapterStr, {
-        preserveChildrenOrder: true,
-        explicitChildren: true,
-        childkey: "children"
-      });
-      const toc = [];
-      this.parseNavMap(tocAst.wrapper.children, toc);
-      this.toc = toc;
+      try {
+        const wrappedChapterStr = `<wrapper>${tocChapterStr.text.replace(/filepos=(\d+)/gi, 'filepos="$1"')}</wrapper>`;
+        const tocAst = await parsexml(wrappedChapterStr, {
+          preserveChildrenOrder: true,
+          explicitChildren: true,
+          childkey: "children"
+        });
+        const toc = [];
+        this.parseNavMap(tocAst.wrapper.children, toc);
+        this.toc = toc;
+      } catch {
+      }
     }
   }
   findTocChapter(referenceStr) {
